.postTitle
  = @user.nickname
  さんのご予約一覧
- @events.each do |event|
  .content
    .content__right
      .content__right__top
        = link_to event.title, event_path(event.id), class: "content__right__top--title"
      .content__right__bottom
        .content__right__bottom--userName
          = link_to event.nickname, event_path(event.user_id), class: "content__right__bottom--userName--btn"
        .content__right__bottom--date
          = event.start_date
